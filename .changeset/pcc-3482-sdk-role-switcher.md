---
"@pantheon-systems/p1-next-sdk": minor
---

**[Feature]** `roleSwitcher: true` now renders a role picker defaulting to `'editor'` when no `userRole` is passed to `createP1EditorClient`. Pass `userRole` explicitly to start the picker at a different role.
