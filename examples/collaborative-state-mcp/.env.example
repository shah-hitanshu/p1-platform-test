# Collaborative State System MCP Server Configuration

# Worker API URL (local development)
WORKER_API_URL=http://localhost:8787

# Agent credentials (from workers/mock-identity.config.json)
# Note: AGENT_ID must be a valid UUID for checkpoint creation
AGENT_ID=a0000000-0000-0000-0000-000000000001
AGENT_API_KEY=test-agent-key-zappy

# Default site and branch for operations
DEFAULT_SITE_ID=site-123
DEFAULT_BRANCH_ID=main
