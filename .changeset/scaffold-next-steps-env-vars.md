---
"@pantheon-systems/create-p1-starter-kit": patch
---

Point the post-scaffold "next steps" output and the README setup table at the credentials the generated app actually requires (`NEXT_PUBLIC_CSS_SITE_ID`, `CSS_API_KEY`) instead of the optional Content Publisher pair.
